<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>CodeDrop - JS Snippets</title>
</head>
<body>
  <h1>Upload a JS Snippet</h1>

  <form action="/upload" method="post" enctype="multipart/form-data">
    <input type="file" name="file" accept=".js" />
    <button type="submit">Upload</button>
  </form>

  <h2>Uploaded Snippets</h2>
  <% if (!files || files.length === 0) { %>
    <p>No snippets uploaded yet.</p>
  <% } else { %>
    <ul>
      <% files.forEach(file => { %>
        <li><a href="/snippet/<%= file %>"><%= file %></a></li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
